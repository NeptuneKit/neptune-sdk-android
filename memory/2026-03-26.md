# 2026-03-26

## 决策

- Maven Central 发布链路从旧 `s01.oss.sonatype.org` 迁移到 Central Portal 兼容端点：
  - release: `https://ossrh-staging-api.central.sonatype.com/service/local/staging/deploy/maven2/`
  - snapshot: `https://central.sonatype.com/repository/maven-snapshots/`
- 保持现有 `maven-publish + signing` 方案，不引入第三方发布插件。
- GitHub Actions 在 release 发布完成后，新增 `manual/upload/defaultRepository/<namespace>` 触发步骤，确保产物在 Central Portal 中可见。
- 新增必须 secret：`MAVEN_CENTRAL_NAMESPACE`（通常为 `groupId` namespace）。

## 验证

- 已通过本地验证：
  - `./gradlew :sdk:test`
  - `./gradlew :sdk:publishToMavenLocal -PVERSION_NAME=0.1.0-SNAPSHOT`
- 本次改动未执行真实远端发布，仍需在仓库 Secrets 配置完成后通过 tag 或 workflow_dispatch 验证。

## 交付物补充

- 新增 `.env.maven-central.example`，用于本地发布变量模板。
- 新增 `scripts/publish-maven-central-local.sh`，支持：
  - `--dry-run` 本地发布校验
  - `--version` 指定版本发布
  - 自动触发 `manual/upload/defaultRepository/<namespace>`
- 已验证 `bash scripts/publish-maven-central-local.sh --dry-run --version 0.1.0-SNAPSHOT` 通过。

## 发布联调结论

- 当 `MAVEN_CENTRAL_NAMESPACE=io.github.linhay` 时，`groupId` 必须与 namespace 对齐；已将构建 group 调整为 `io.github.linhay`。
- `dry_run=true` 且 release 版本（`0.1.0`）需要注入签名 secrets，否则会触发 signing gate 失败。
- 正式发布流程中，`manual/upload` 失败的关键根因是 Central 校验阶段找不到签名公钥；公钥上传并完成传播后可恢复。
- 已完成一次成功发布工作流（包含 `Publish` 与 `manual upload` 成功）。
